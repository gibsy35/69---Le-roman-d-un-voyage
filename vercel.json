{
  "version": 2,
  "buildCommand": "vite build",
  "builds": [
    {
      "src": "server.ts",
      "use": "@vercel/node",
      "config": {
        "includeFiles": ["dist/public/**", "types.ts"]
      }
    }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "/server.ts" }
  ]
}
